# Claude COWORK · Ad System Configuration

meta:
  access_token: "YOUR_META_API_TOKEN"  # ได้จาก https://developers.facebook.com
  base_url: "https://graph.facebook.com/v18.0"
  
openai:
  api_key: "YOUR_OPENAI_API_KEY"  # ได้จาก https://platform.openai.com
  model: "gpt-4o"
  temperature: 0.7

languages:
  primary: "th"
  secondary: "en"

output:
  format: "markdown"
  directory: "./reports"
  
logging:
  level: "INFO"
  file: "zadsystem.log"
