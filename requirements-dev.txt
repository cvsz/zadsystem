# Test/security-only dependencies are pinned to keep CI behavior reproducible.
# pytest 9.0.3 includes the CVE-2025-71176 insecure temporary-directory fix.
pytest==9.0.3
# pip-audit 2.10.1 is the current release used to fail CI on known vulnerable Python packages.
pip-audit==2.10.1
